<!DOCTYPE html>
<html>
<head>
  <title>Whiteboard</title>
  <%= stylesheet_link_tag :flutie %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <style type="text/css"> 
  /* These are styles to make the page look nice */

  body { background-color: #222;}
  .content {
    background: #fff;
    padding: 40px;
    margin: 40px auto;
    margin-bottom:5px;
    width: 880px;
    -moz-border-radius: 24px;
    -webkit-border-radius: 24px;
  }
  
  #masthead {
    font-weight:bold;
    color:#ffffff;
    font-size:120px;
    padding: 0px;
    margin: 0px auto -60px auto;
    width: 880px;
    text-align:center;
  }
  #footer {
    color:#ccc;
    width:880px;
    text-align:left;
    margin:auto;
  }
  </style>
</head>
<body>
  <div id="masthead">WHITEBOARD</div>
  <div class="content">
    <% if notice %><div class="notice"><%= notice %></div><% end %>
    <% if alert %><div class="error"><%= alert %></div><% end %>
    <div style="display:block;clear:both">&nbsp;</div>
    <div style="float:left;width:150px">
      <% if user_signed_in? %>
        <ul>
          <li><%= link_to "Your courses", courses_path %>
          <li><%= link_to "Sign out", destroy_user_session_path %>
        </ul>
      <% else %>
        <div>Currently in private beta.</div>
      <% end %>
    </div>
    <div style="padding-left:160px"><%= yield %></div>
  </div>
  <div id="footer">ABOUT // FAQ // LEGAL</div>
</body>
</html>
