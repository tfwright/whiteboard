<%= form_for(@upload, :html => {:multipart => true}, :url => course_uploads_path(Course.find(params[:course_id]))) do |f| %>
  <% if @upload.errors.any? %>
  <div class="error">
    <h3><%= pluralize(@upload.errors.count, "error") %> prevented this course from being saved:</h3>
    <% @upload.errors[:base].each do |error| %>
      <%= error %>
    <% end %>
  </div>
  <% end %>
  <ol>
    <li class="string required <%= "error" if @upload.errors[:name].any? %>"> 
      <%= f.label :name, raw("Name<abbr>*</abbr>") %><br />
      <%= f.text_field :name %>
      <% @upload.errors[:name].each do |error| %>
         <p class="inline-errors"><%= error %></p> 
      <% end %>
    </li>
    <li class="string required <%= "error" if @upload.errors[:description].any? %>"> 
      <%= f.label :description, raw("Description<abbr>*</abbr>") %><br />
      <%= f.text_field :description %>
      <% @upload.errors[:description].each do |error| %>
         <p class="inline-errors"><%= error %></p> 
      <% end %>
    </li>
    <li class="file required"> 
      <%= f.label :attached, raw("Attachment<abbr>*</abbr>") %><br />
      <%= f.file_field :attached %>
      <p class="inline-hints">currently only word documents and pdfs are allowed</p> 
      <% @upload.errors[:attached_file_name].each do |error| %>
         <p class="inline-errors">File <%= error %></p> 
      <% end %>
      <% @upload.errors[:attached_file_size].each do |error| %>
         <p class="inline-errors"><%= error %></p> 
      <% end %>
      <% @upload.errors[:attached_content_type].each do |error| %>
         <p class="inline-errors"><%= error %></p> 
      <% end %>
    </li>
  </ol>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
